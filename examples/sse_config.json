{
    "sseProxy": {
        "allowedServers": ["uvx", "mcp-server-fetch"],
        "authenticate": {
            "bearer": {
                "token": "your token"
            }
        },
        "address": "127.0.0.1",
        "port": 3000
    },
    "mcpServers": {
      "fetch": {
        "command": "uvx",
        "args": ["mcp-server-fetch"]
      },
      "filesystem": {
        "command": "npx",
        "args": [
          "-y",
          "@modelcontextprotocol/server-filesystem",
          "/tmp"
        ]
      }
    }
}